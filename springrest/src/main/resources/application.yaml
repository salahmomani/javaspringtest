spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springtest?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: salah
    password: salah462002#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
